<%= render partial: "shared/admin_dashboard" %>

<h1><%= @merchant.name %></h1>
<div class="row">
  <div class="col">

  <% if @merchant.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@merchant.errors.count, "error") %> prohibited
        this merchant from being saved:
      </h2>
      <ul>
        <% @merchant.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  </div>
</div>


<div class="row">
  <div class="col">
    <%= link_to 'update merchant', edit_admin_merchant_path(@merchant.id), class: 'btn btn-primary' %>
  </div>
</div>
